# Dependances Python (3.10+)
PyMuPDF>=1.24
openpyxl>=3.1
PyYAML>=6.0
reportlab>=4.0

# Backend API
fastapi>=0.115
uvicorn>=0.34
python-multipart>=0.0.18

# Multi-format extraction
pdfplumber>=0.11          # fallback texte + extraction tableaux
ezdxf>=1.3                # support DXF (TEXT, MTEXT, LWPOLYLINE, INSERT)
opencv-python>=4.10       # preprocessing raster (deskew, contraste)
numpy>=1.24               # dependencies opencv + calculs

# OCR local (fallback pour PDF scannes)
rapidocr-onnxruntime>=1.3 # OCR local 100% CPU
onnxruntime>=1.17         # runtime ONNX pour RapidOCR

# IFC (format BIM le plus fiable)
ifcopenshell>=0.7.0       # extraction IFC (IfcBeam, IfcColumn, etc.)

# Geometrie
shapely>=2.0              # calculs geometriques (segments, intersections)

# Clustering spatial
scikit-learn>=1.4         # DBSCAN pour clustering par niveau

# Conversion DWG (optionnel — necessite ODA File Converter installe)
# dwg2dxf                 # convertisseur DWG->DXF (CLI)

# Optionnel — vision API (extract_plan.py --vision)
# anthropic>=0.40

# Optionnel — OCR avance
# paddleocr>=2.9          # OCR alternatif (lourd mais precis)
# easyocr>=1.7            # OCR multi-langue
